@page "/settings/app"
@attribute [Microsoft.AspNetCore.Authorization.Authorize]

<PageTitle>App Settings · BornoBit.Restaurant</PageTitle>

<div class="bo-page">
    <PageHeader Title="App Settings" Description="Personal preferences for this browser. These choices are saved locally and don't affect other users." />

    <div class="bo-grid-2" style="align-items:start;">
        <div class="bo-card">
            <div class="bo-card-header">
                <div>
                    <h3 class="bo-card-title">Theme color</h3>
                    <p class="bo-card-subtitle">Pick the accent color used across buttons, links, and highlights.</p>
                </div>
            </div>
            <ThemePicker />
        </div>

        <div class="bo-card">
            <div class="bo-card-header">
                <div>
                    <h3 class="bo-card-title">About</h3>
                    <p class="bo-card-subtitle">More personalization options will land here.</p>
                </div>
            </div>
            <div class="bo-text-muted" style="font-size: var(--bo-font-sm); line-height:1.6;">
                Theme preferences are stored in your browser's local storage. Clearing site data or switching browsers will reset them.
            </div>
        </div>
    </div>
</div>
