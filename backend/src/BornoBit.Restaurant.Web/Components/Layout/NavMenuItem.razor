@* Legacy stub kept for compatibility. NavMenu.razor now renders the tree directly. *@
@code {
    [Parameter] public BornoBit.Restaurant.Application.Menus.MenuItemDto? Item { get; set; }
    [Parameter] public bool ForceExpand { get; set; }
}
