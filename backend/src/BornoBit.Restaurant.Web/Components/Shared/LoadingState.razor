<div class="bo-loading">
    <div class="bo-spinner"></div>
    @if (!string.IsNullOrEmpty(Message))
    {
        <div style="margin-top: 8px; font-size: var(--bo-font-sm);">@Message</div>
    }
</div>

@code {
    [Parameter] public string? Message { get; set; }
}
