// Reserved for future toast keyboard / hover-pause helpers.
// Currently a no-op placeholder so App.razor's <script> tag doesn't 404.
(function () {
    if (window.hcToast) return;
    window.hcToast = { ready: true };
})();
